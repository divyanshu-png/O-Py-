# Backend (Django)
Django==6.0.4
djangorestframework==3.17.1
django-cors-headers==4.9.0

# SQL Server support (only needed if OPY_USE_SQLITE is unset/0 - see myproject/settings.py)
mssql-django==1.7.1
pyodbc==5.3.0

# Frontend (Streamlit) + API client
streamlit==1.57.0
streamlit-monaco==0.1.3
requests==2.33.1

# AI question generation / feedback (examiner/ai_manager.py)
transformers==5.8.0
torch==2.12.1
accelerate==1.14.0

# CPU-only torch install (much smaller/faster than the default GPU wheel):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
